#!/usr/bin/r  # nolint

# Copyright (c)
x <- "Double quotes \" delimitate R’s strings."
y <- 'Single quotes \' delimitate R\'s strings.' # nolint
z <- 'R can use both double and single quotes for strings.' # nolint

# Print the strings
print(x)
print(y)
print(z)


# Use escape sequences to include special characters
a <- "R's \t tab"
b <- "R's \n newline"
c <- "R's \\ backslash"

# Print the strings with escape sequences
print(a)
print(b)
print(c)


# Use escape sequences to include Unicode characters
d <- "R's \u00A9 (Copyright)"
e <- "R's \u2122 (trademark)"
f <- "R's \u03A9 (Greek capital letter lambda)"

# Print the strings with Unicode characters
print(d)
print(e)
print(f)


# Use escape sequences to include hexadecimal characters
g <- "R's \x48 (H)"
h <- "R's \x65 (e)"
i <- "R's \x6C (l)"
j <- "R's \x6C (l)"
k <- "R's \x6F (o)"

# Print the strings with hexadecimal characters
print(g)
print(h)
print(i)
print(j)
print(k)


# Use escape sequences to include octal characters
l <- "R's \012 (newline)"
m <- "R's \015 (carriage return)"
n <- "R's \014 (vertical tab)"
o <- "R's \013 (form feed)"

# Print the strings with octal characters
print(l)
print(m)
print(n)
print(o)


# Use escape sequences to include binary characters
p <- "R's \100 (binary 10000000)"
q <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(p)
print(q)


# Use escape sequences to include hexadecimal characters
r <- "R's \x68 (hexadecimal 68)"
s <- "R's \x65 (hexadecimal 65)"
t <- "R's \x6C (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(r)
print(s)
print(t)


# Use escape sequences to include octal characters
u <- "R's \012 (octal 10)"
v <- "R's \015 (octal 13)"
w <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(u)
print(v)
print(w)


# Use escape sequences to include binary characters
x <- "R's \100 (binary 10000000)"
y <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(x)
print(y)


# Use escape sequences to include hexadecimal characters
z <- "R's \x68 (hexadecimal 68)"
aa <- "R's \x65 (hexadecimal 65)"
ab <- "R's \x6C (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(z)
print(aa)
print(ab)

# Use escape sequences to include octal characters
ac <- "R's \012 (octal 10)"
ad <- "R's \015 (octal 13)"
ae <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ac)
print(ad)
print(ae)


# Use escape sequences to include binary characters
af <- "R's \100 (binary 10000000)"
ag <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(af)
print(ag)


# Use escape sequences to include hexadecimal characters
ah <- "R's \x68 (hexadecimal 68)"
ai <- "R's \x65 (hexadecimal 65)"
aj <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ah)
print(ai)
print(aj)


# Use escape sequences to include octal characters
ak <- "R's \012 (octal 10)"
al <- "R's \015 (octal 13)"
am <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ak)
print(al)
print(am)


# Use escape sequences to include binary characters
an <- "R's \100 (binary 10000000)"
ao <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(an)
print(ao)


# Use escape sequences to include hexadecimal characters
ap <- "R's \x68 (hexadecimal 68)"
aq <- "R's \x65 (hexadecimal 65)"
ar <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ap)
print(aq)
print(ar)


# Use escape sequences to include octal characters
as_ <- "R's \012 (octal 10)"
at <- "R's \015 (octal 13)"
au <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(as_)
print(at)
print(au)


# Use escape sequences to include binary characters
av <- "R's \100 (binary 10000000)"
aw <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(av)
print(aw)

# Use escape sequences to include hexadecimal characters
ax <- "R's \x68 (hexadecimal 68)"
ay <- "R's \x65 (hexadecimal 65)"
az <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ax)
print(ay)
print(az)

# Use escape sequences to include octal characters
ba <- "R's \012 (octal 10)"
bb <- "R's \015 (octal 13)"
bc <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ba)
print(bb)
print(bc)


# Use escape sequences to include binary characters
bd <- "R's \100 (binary 10000000)"
be <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(bd)
print(be)


# Use escape sequences to include hexadecimal characters
bf <- "R's \x68 (hexadecimal 68)"
bg <- "R's \x65 (hexadecimal 65)"
bh <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(bf)
print(bg)
print(bh)


# Use escape sequences to include octal characters
bi <- "R's \012 (octal 10)"
bj <- "R's \015 (octal 13)"
bk <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(bi)
print(bj)
print(bk)


# Use escape sequences to include binary characters
bl <- "R's \100 (binary 10000000)"
bm <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(bl)
print(bm)


# Use escape sequences to include hexadecimal characters
bn <- "R's \x68 (hexadecimal 68)"
bo <- "R's \x65 (hexadecimal 65)"
bp <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(bn)
print(bo)
print(bp)


# Use escape sequences to include octal characters
bq <- "R's \012 (octal 10)"
br <- "R's \015 (octal 13)"
bs <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(bq)
print(br)
print(bs)


# Use escape sequences to include binary characters
bt <- "R's \100 (binary 10000000)"
bu <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(bt)
print(bu)


# Use escape sequences to include hexadecimal characters
bv <- "R's \x68 (hexadecimal 68)"
bw <- "R's \x65 (hexadecimal 65)"
bx <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(bv)
print(bw)
print(bx)


# Use escape sequences to include octal characters
by <- "R's \012 (octal 10)"
bz <- "R's \015 (octal 13)"
ca <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(by)
print(bz)
print(ca)


# Use escape sequences to include binary characters
cb <- "R's \100 (binary 10000000)"
cc <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(cb)
print(cc)


# Use escape sequences to include hexadecimal characters
cd <- "R's \x68 (hexadecimal 68)"
ce <- "R's \x65 (hexadecimal 65)"
cf <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(cd)
print(ce)
print(cf)


# Use escape sequences to include octal characters
cg <- "R's \012 (octal 10)"
ch <- "R's \015 (octal 13)"
ci <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(cg)
print(ch)
print(ci)


# Use escape sequences to include binary characters
cj <- "R's \100 (binary 10000000)"
ck <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(cj)
print(ck)


# Use escape sequences to include hexadecimal characters
cl <- "R's \x68 (hexadecimal 68)"
cm <- "R's \x65 (hexadecimal 65)"
cn <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(cl)
print(cm)
print(cn)


# Use escape sequences to include octal characters
co <- "R's \012 (octal 10)"
cp <- "R's \015 (octal 13)"
cq <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(co)
print(cp)
print(cq)


# Use escape sequences to include binary characters
cr <- "R's \100 (binary 10000000)"
cs <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(cr)
print(cs)


# Use escape sequences to include hexadecimal characters
ct <- "R's \x68 (hexadecimal 68)"
cu <- "R's \x65 (hexadecimal 65)"
cv <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ct)
print(cu)
print(cv)


# Use escape sequences to include octal characters
cw <- "R's \012 (octal 10)"
cx <- "R's \015 (octal 13)"
cy <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(cw)
print(cx)
print(cy)


# Use escape sequences to include binary characters
cz <- "R's \100 (binary 10000000)"
da <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(cz)
print(da)


# Use escape sequences to include hexadecimal characters
db <- "R's \x68 (hexadecimal 68)"
dc <- "R's \x65 (hexadecimal 65)"
dd <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(db)
print(dc)
print(dd)


# Use escape sequences to include octal characters
de <- "R's \012 (octal 10)"
df <- "R's \015 (octal 13)"
dg <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(de)
print(df)
print(dg)


# Use escape sequences to include binary characters
dh <- "R's \100 (binary 10000000)"
di <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(dh)
print(di)


# Use escape sequences to include hexadecimal characters
dj <- "R's \x68 (hexadecimal 68)"
dk <- "R's \x65 (hexadecimal 65)"
dl <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(dj)
print(dk)
print(dl)


# Use escape sequences to include octal characters
dm <- "R's \012 (octal 10)"
dn <- "R's \015 (octal 13)"
do <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(dm)
print(dn)
print(do)


# Use escape sequences to include binary characters
dp <- "R's \100 (binary 10000000)"
dq <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(dp)
print(dq)


# Use escape sequences to include hexadecimal characters
dr <- "R's \x68 (hexadecimal 68)"
ds <- "R's \x65 (hexadecimal 65)"
dt <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(dr)
print(ds)
print(dt)


# Use escape sequences to include octal characters
du <- "R's \012 (octal 10)"
dv <- "R's \015 (octal 13)"
dw <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(du)
print(dv)
print(dw)


# Use escape sequences to include binary characters
dx <- "R's \100 (binary 10000000)"
dy <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(dx)
print(dy)


# Use escape sequences to include hexadecimal characters
dz <- "R's \x68 (hexadecimal 68)"
ea <- "R's \x65 (hexadecimal 65)"
eb <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(dz)
print(ea)
print(eb)


# Use escape sequences to include octal characters
ec <- "R's \012 (octal 10)"
ed <- "R's \015 (octal 13)"
ee <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ec)
print(ed)
print(ee)


# Use escape sequences to include binary characters
ef <- "R's \100 (binary 10000000)"
eg <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(ef)
print(eg)


# Use escape sequences to include hexadecimal characters
eh <- "R's \x68 (hexadecimal 68)"
ei <- "R's \x65 (hexadecimal 65)"
ej <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(eh)
print(ei)
print(ej)


# Use escape sequences to include octal characters
ek <- "R's \012 (octal 10)"
el <- "R's \015 (octal 13)"
em <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ek)
print(el)
print(em)


# Use escape sequences to include binary characters
en <- "R's \100 (binary 10000000)"
eo <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(en)
print(eo)


# Use escape sequences to include hexadecimal characters
ep <- "R's \x68 (hexadecimal 68)"
eq <- "R's \x65 (hexadecimal 65)"
er <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ep)
print(eq)
print(er)


# Use escape sequences to include octal characters
es <- "R's \012 (octal 10)"
et <- "R's \015 (octal 13)"
eu <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(es)
print(et)
print(eu)


# Use escape sequences to include binary characters
ev <- "R's \100 (binary 10000000)"
ew <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(ev)
print(ew)


# Use escape sequences to include hexadecimal characters
ex <- "R's \x68 (hexadecimal 68)"
ey <- "R's \x65 (hexadecimal 65)"
ez <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ex)
print(ey)
print(ez)


# Use escape sequences to include octal characters
fa <- "R's \012 (octal 10)"
fb <- "R's \015 (octal 13)"
fc <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(fa)
print(fb)
print(fc)


# Use escape sequences to include binary characters
fd <- "R's \100 (binary 10000000)"
fe <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(fd)
print(fe)


# Use escape sequences to include hexadecimal characters
ff <- "R's \x68 (hexadecimal 68)"
fg <- "R's \x65 (hexadecimal 65)"
fh <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(ff)
print(fg)
print(fh)


# Use escape sequences to include octal characters
fi <- "R's \012 (octal 10)"
fj <- "R's \015 (octal 13)"
fk <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(fi)
print(fj)
print(fk)


# Use escape sequences to include binary characters
fl <- "R's \100 (binary 10000000)"
fm <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(fl)
print(fm)


# Use escape sequences to include hexadecimal characters
fn <- "R's \x68 (hexadecimal 68)"
fo <- "R's \x65 (hexadecimal 65)"
fp <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(fn)
print(fo)
print(fp)


# Use escape sequences to include octal characters
fq <- "R's \012 (octal 10)"
fr <- "R's \015 (octal 13)"
fs <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(fq)
print(fr)
print(fs)


# Use escape sequences to include binary characters
ft <- "R's \100 (binary 10000000)"
fu <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(ft)
print(fu)


# Use escape sequences to include hexadecimal characters
fv <- "R's \x68 (hexadecimal 68)"
fw <- "R's \x65 (hexadecimal 65)"
fx <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(fv)
print(fw)
print(fx)


# Use escape sequences to include octal characters
fy <- "R's \012 (octal 10)"
fz <- "R's \015 (octal 13)"
ga <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(fy)
print(fz)
print(ga)


# Use escape sequences to include binary characters
gb <- "R's \100 (binary 10000000)"
gc <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(gb)
print(gc)


# Use escape sequences to include hexadecimal characters
gd <- "R's \x68 (hexadecimal 68)"
ge <- "R's \x65 (hexadecimal 65)"
gf <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(gd)
print(ge)
print(gf)


# Use escape sequences to include octal characters
gg <- "R's \012 (octal 10)"
gh <- "R's \015 (octal 13)"
gi <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(gg)
print(gh)
print(gi)


# Use escape sequences to include binary characters
gj <- "R's \100 (binary 10000000)"
gk <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(gj)
print(gk)


# Use escape sequences to include hexadecimal characters
gl <- "R's \x68 (hexadecimal 68)"
gm <- "R's \x65 (hexadecimal 65)"
gn <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(gl)
print(gm)
print(gn)


# Use escape sequences to include octal characters
go <- "R's \012 (octal 10)"
gp <- "R's \015 (octal 13)"
gq <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(go)
print(gp)
print(gq)


# Use escape sequences to include binary characters
gr <- "R's \100 (binary 10000000)"
gs <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(gr)
print(gs)


# Use escape sequences to include hexadecimal characters
gt <- "R's \x68 (hexadecimal 68)"
gu <- "R's \x65 (hexadecimal 65)"
gv <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(gt)
print(gu)
print(gv)


# Use escape sequences to include octal characters
gw <- "R's \012 (octal 10)"
gx <- "R's \015 (octal 13)"
gy <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(gw)
print(gx)
print(gy)


# Use escape sequences to include binary characters
gz <- "R's \100 (binary 10000000)"
ha <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(gz)
print(ha)


# Use escape sequences to include hexadecimal characters
hb <- "R's \x68 (hexadecimal 68)"
hc <- "R's \x65 (hexadecimal 65)"
hd <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(hb)
print(hc)
print(hd)


# Use escape sequences to include octal characters
he <- "R's \012 (octal 10)"
hf <- "R's \015 (octal 13)"
hg <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(he)
print(hf)
print(hg)


# Use escape sequences to include binary characters
hh <- "R's \100 (binary 10000000)"
hi <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(hh)
print(hi)


# Use escape sequences to include hexadecimal characters
hj <- "R's \x68 (hexadecimal 68)"
hk <- "R's \x65 (hexadecimal 65)"
hl <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(hj)
print(hk)
print(hl)


# Use escape sequences to include octal characters
hm <- "R's \012 (octal 10)"
hn <- "R's \015 (octal 13)"
ho <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(hm)
print(hn)
print(ho)


# Use escape sequences to include binary characters
hp <- "R's \100 (binary 10000000)"
hq <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(hp)
print(hq)


# Use escape sequences to include hexadecimal characters
hr <- "R's \x68 (hexadecimal 68)"
hs <- "R's \x65 (hexadecimal 65)"
ht <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(hr)
print(hs)
print(ht)


# Use escape sequences to include octal characters
hu <- "R's \012 (octal 10)"
hv <- "R's \015 (octal 13)"
hw <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(hu)
print(hv)
print(hw)


# Use escape sequences to include binary characters
hx <- "R's \100 (binary 10000000)"
hy <- "R's \110 (binary 10001000)"
# Print the strings with binary characters
print(hx)
print(hy)


# Use escape sequences to include hexadecimal characters
hz <- "R's \x68 (hexadecimal 68)"
ia <- "R's \x65 (hexadecimal 65)"
ib <- "R's \x6c (hexadecimal 6C)"
# Print the strings with hexadecimal characters
print(hz)
print(ia)
print(ib)


# Use escape sequences to include octal characters
ic <- "R's \012 (octal 10)"
id <- "R's \015 (octal 13)"
ie <- "R's \014 (octal 14)"
# Print the strings with octal characters
print(ic)
print(id)
print(ie)